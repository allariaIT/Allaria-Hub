# Proxy para proyectos sandbox — rutea al sandbox-nginx en .101
server {
    listen 3097;
    server_name proyectos-sandbox.allaria.xyz;

    location / {
        proxy_pass http://172.30.200.101:3099;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}

server {
    listen 3097;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    resolver 169.254.20.10 valid=10s ipv6=off;

    location /api/ {
        set $backend "http://back.allaria-hub.svc.cluster.local:3098";
        proxy_pass $backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
